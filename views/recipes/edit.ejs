<!DOCTYPE html>
<html lang="en">
    <%- include('../includes/head', {title: 'Edit Recipe'}) %>
<body>
    <h2 class="heading">Edit a Recipe</h2>

    <form action="/recipes/<%=recipe.slug %>" method="post">
        <input type="text" value="<%= recipe.name %>" name="name" placeholder="name" required> <br>
        <input type="text" value="<%= recipe.slug %>" name="slug" placeholder="slug" required> <br>
        <button>Save</button>
    </form>
</body>
</html>